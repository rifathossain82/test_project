import 'package:flutter/material.dart';

class notification_tab extends StatelessWidget {
  const notification_tab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text('Notification Tab',style: TextStyle(fontSize: 30),)),
    );
  }
}
