import 'package:flutter/material.dart';

class share_tab extends StatelessWidget {
  const share_tab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text('Share Tab',style: TextStyle(fontSize: 30),)),
    );
  }
}
